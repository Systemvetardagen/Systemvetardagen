import React from 'react';

interface CompanySignupProps {
    temp: string;
}

const CompanySignup: React.FC<CompanySignupProps> = ({ temp }) => {
    //todo
    return <div>{temp}</div>;
};

export default CompanySignup;
